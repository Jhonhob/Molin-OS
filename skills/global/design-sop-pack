---
name: design-sop-pack
description: 墨图设计 Agent SOP 技能包 — 视觉内容标准化生产流水线，含封面/UI/广告图/品牌视觉
category: molin-org
version: 1.0.0
tags: [sop, design, visual, cover, brand, creative]
trigger: 所有设计任务（封面/Banner/UI/广告图/品牌视觉）必须先加载此技能
---

# 墨图设计 Agent SOP 技能包

## 适用 Agent
- 墨图设计 (designer.py)
- 集成技能：ai-image-gen, claude-design, comfyui, excalidraw, popular-web-designs, sketch

---

## 一、Lead SOP（设计需求获取）

### 需求来源

| 来源 | 触发方式 | 优先级 | 典型产出 |
|------|----------|--------|----------|
| 内容 Agent 管道 | 内容产出附带「配图需求」标记 | P0 | 封面图/配图 |
| Marketing 选题会 | 排期文件中的配图需求 | P1 | 广告图/Banner |
| 品牌运营 | 品牌视觉更新 | P1 | Logo/品牌素材 |
| 临时需求（飞书/直接对话） | 手动触发 | P2 | 单次设计 |

### 需求格式化

所有设计需求必须结构化为：

```yaml
design_brief:
  type: "cover / banner / ui / ad / brand / diagram / mockup"
  platform: "xiaohongshu / douyin / web / wechat / general"
  content_topic: "关联内容主题（如有）"
  style: "写实 / 插画 / 3D / 极简 / 国风 / 品牌色系"
  size: "宽度×高度 px"  # 或平台标准尺寸代码
  key_text: "封面/海报上必须出现的文字"
  color_scheme: "主色 / 强调色 / 背景色"
  reference: "参考风格图URL或描述"
  brand_level: "L0-auto / L1-notify / L2-approve"
```

### 标准尺寸库（自动映射）

| 平台 | 类型 | 尺寸 (px) | 比例 |
|------|------|-----------|------|
| 小红书 | 封面 | 1080×1440 | 3:4 |
| 小红书 | 配图 | 1080×1080 | 1:1 |
| 抖音 | 封面 | 1080×1920 | 9:16 |
| 公众号 | 封面 | 900×500 | 16:9 |
| 公众号 | 配图 | 1080×1080 | 1:1 |
| SEO 站点 | 特色图 | 1200×630 | 1.9:1 |
| LINE 推播 | 卡片图 | 1024×512 | 2:1 |
| 广告图 | 通用 | 1200×628 | 1.9:1 |
| 广告图 | 方图 | 1080×1080 | 1:1 |

---

## 二、Execution SOP（设计执行流程）

### 设计生产流水线

```
需求接收 → 概念构思 → 初稿生成 → 质检自检 → 修正调整 → 终稿输出
```

### Step 1: 需求解析
- 从设计 brief 中提取关键参数（类型/平台/风格/尺寸/文字）
- 如果需求不完整，补充缺失参数（平台默认 / 风格推断 / 尺寸映射）
- 检查品牌一致性要求

### Step 2: 概念构思
根据设计类型选择生成路径：

| 设计类型 | 推荐工具 | 说明 |
|----------|----------|------|
| 封面图/配图 | ai-image-gen | qwen-image-2.0-pro 或 GPT Image 2 |
| 广告图 | ai-image-gen | 同封面，额外考虑 CTA 文字布局 |
| UI 界面 | popular-web-designs + claude-design | 基于 54 设计系统 |
| 信息图 | baoyu-infographic | 21 布局 × 21 风格 |
| 手绘风图 | excalidraw | 流程图/架构图 |
| 像素风图 | pixel-art | NES/Game Boy/PICO-8 风格 |
| SVG 图表 | architecture-diagram | 深色主题基础架构图 |
| HTML 原型 | sketch | 2-3 个变体快速对比 |

### Step 3: 初稿生成

Prompt 工程规则：
- 所有生图 prompt 必须包含品牌色系统一前缀
- 封面必须预留 CTA 文字位置（上方 1/3 留白或纯色区域）
- 多平台需求：首先生成最大尺寸版本，再等比缩放
- 分辨率：不低于 1080px 短边

### Step 4: 自检修正（QA 前置）

产出后先做人工模拟检查：
- 文字是否清晰可读（特别是封面上的叠加文字）
- 颜色是否与品牌色卡匹配
- 是否有明显 AI 瑕疵（手指/扭曲/多余物体）
- 尺寸是否符合目标平台要求
- 文件格式是否正确

### Step 5: 终稿输出

输出格式：
- 图片文件 → 按约定命名保存：`{type}_{platform}_{content_topic}_{timestamp}.{ext}`
- 元数据 → 写在同目录 `_meta.json`：关联内容 ID、设计参数、品牌合规通过情况
- 归档 → Obsidian `Agents/墨图设计/产出/{date}/`

---

## 三、QA SOP（视觉质量质检）

### 评分维度

| 维度 | 权重 | 检查项 |
|------|------|--------|
| 品牌一致性 | 25% | 品牌色准确、Logo正确、字体统一 |
| 视觉吸引力 | 20% | 构图、色彩和谐、视觉层次 |
| 平台适配 | 20% | 尺寸正确、平台规范符合 |
| 文字可读性 | 15% | 关键文字清晰、对比度足够 |
| CTA 有效性 | 10% | 按钮/文字引导明确 |
| 原创性 | 10% | 非明显模板化、有差异化 |

### 否决条件（直接 0 分）
- Logo/品牌标识变形或使用错误版本
- 包含敏感/违规视觉元素
- 关键文字被裁切或不可读
- 分辨率低于平台最低要求

### 阈值动作

| 分数 | 动作 |
|------|------|
| ≥ 85 | ✅ 直接交付 |
| 70-84 | 🔄 修正低分维度后交付 |
| 60-69 | ⚠️ 人工确认 |
| < 60 | 🚫 重新生成 |

---

## 四、Escalation SOP

| 场景 | 触发条件 | 动作 | 治理级别 |
|------|----------|------|----------|
| 生图 API 失败 | 连续 3 次生成失败 | 切换备选生图工具 | L0 自动 |
| 品牌严重偏离 | QA 品牌分 < 60 | 暂停交付，通知审核 | L2 审批 |
| 合规风险 | 检测到敏感视觉元素 | 立即删除，通知 Founder | L2 审批 |
| 需求不明确 | brief 缺失关键字段 | 通知发起方补充 | L1 通知 |

---

## 五、Cron 经营节奏

| 时间 | 任务 | 产出 |
|------|------|------|
| 每日 08:00 | 检查内容排期中的配图需求 | 需求清单 |
| 跟随内容生产 | 按需执行封面/配图生成 | 图片文件 |
| 22:00 | 设计产出复盘（附在内容复盘后） | KPI 指标补充 |
| 周日 21:00 | 视觉风格趋势分析 + 模板更新 | 风格优化建议 |

---

## 六、与内容 Agent 的集成

### 设计-内容联动流程

```
内容 Agent 完成稿件
    ↓ Step 6 平台适配时标记「配图需求」
设计 Agent 接收 brief
    ↓ 执行设计 SOP
封面/配图就绪
    ↓ 通过 QA
与内容合并交付
```

集成点：
1. 内容稿件 frontmatter 中的 `needs_design: true` 触发设计流程
2. 设计产出写入 relay/{content_id}_cover.png + relay/{content_id}_meta.json
3. 分发层从 relay/ 拉取内容+配图一起发布

---

## 七、参考

- 品牌色卡：参考品牌指南
- 设计模板：各对应 skill
- 输出存档：Obsidian `Agents/墨图设计/`
- Agent 模板：`skill_view('agent-sop-template')`
- 内容联动：`skill_view('content-sop-pack')` Execution SOP Step 6
