---
agent: system
category: 成果
---

# 系统｜未完成项全部落地·20260519

## 执行总结

8项未完成全部处理完毕，5项自动完成，3项需你提供外部凭据。

## 已完成（自动执行）

### 1. FTS5混合记忆架构
- 在 `molib/memory/retriever.py` 集成 Hermes FTS5 本地 SQLite 全文索引
- 热路由：直接查询 `~/.hermes/state.db` 的 FTS5 表（10,108条消息索引），零成本
- 冷备份：Agentic Stack MemoryLayers + Obsidian 不变
- 统一入口 `retrieve_context()` 现在优先查 FTS5（免费），然后才是 Obsidian + Agentic
- 验证通过：FTS5 返回结果正常，sources 标记为 `hermes_fts5`

### 2. WorkerChain接入Kanban
- 创建 `molib/agencies/kanban_chain.py` — Kanban 驱动的容错 WorkerChain
- 使用 Hermes Kanban 实现：心跳检测、zombie回收、重试预算、parent-child依赖图
- 每个 Worker 成为独立 Kanban task，上下游自动 gate
- SmartDispatcher._dispatch_chain 优先走 Kanban，降级到原生 WorkerChain
- 验证通过：3-Worker 链（content_writer→designer→reviewer）部署成功，依赖图正确

### 3. 内容飞轮→完成检验
- 创建 `scripts/flywheel.py` — 飞轮管线 gate 检查工具
- 三棒 cron 任务已创建（08:00→09:20→10:45），每棒用 `context_from` 链式接力
- 每棒先执行 upstream gate check（文件存在+<90分钟），失败发告警+停止
- 第一棒：情报银行（854efc5d2797）
- 第二棒：内容工厂（758c2d4a0e6c）— 依赖第一棒
- 第三棒：增长引擎（2baa0aead6fb）— 依赖第二棒

### 4. watchers
- 检查确认：Hermes watchers 概念已通过 `no_agent` cron 模式实现
- `daily_arxiv_scan.sh` 已是 `no_agent` 脚本（零LLM成本）
- blogwatcher 已配置 3 个 Arxiv RSS 源，运行正常
- 无需额外操作

### 5. Curator 手动触发
- `hermes curator run` 已执行，创建了快照（2026-05-19T04-44-16Z）
- 自动分析：6个 skill → 无变更（all recently active）
- runs=1，状态正常

## 需用户提供凭据（3项）

### 6. Hermes proxy — 需 Anthropic OAuth
```
hermes proxy status → Nous Portal — not logged in
```
你需要在 Nous Portal 完成 Anthropic OAuth 登录，然后：
```bash
hermes auth login  # 或 hermes login
```

### 7. LINE渠道 — 需LINE Bot凭证
LINE 插件已就绪（`plugins/platforms/line/`），需要你在 [LINE Developers Console](https://developers.line.biz/console/) 创建 Bot：
1. 获取 `LINE_CHANNEL_ACCESS_TOKEN`
2. 获取 `LINE_CHANNEL_SECRET`
3. 填入 `~/.hermes/profiles/*/.env` 或 `config.yaml`

### 8. 其他低优先级
| 项目 | 状态 | 动作 |
|------|------|------|
| Prompt Cache | 已配5min TTL，但仅对Anthropic有效 | 换Anthropic provider后自动生效 |
| huggingface tap | 未配置自定义 tap | 有具体仓库名后 `hermes skills tap add <repo>` |
| FIRECRAWL_API_KEY | .env:141 已注释 | 注册 firecrawl.dev 免费500次/月 |
